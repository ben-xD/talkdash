{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "start": "bun run index.ts",
    "dev": "bun run --watch index.ts",
    "debug": "bun run --inspect --watch index.ts"
  },
  "devDependencies": {
    "@flydotio/dockerfile": "latest",
    "bun-types": "^1.0.6"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "uuid": "^9.0.1",
    "zod": "^3.22.4"
  }
}