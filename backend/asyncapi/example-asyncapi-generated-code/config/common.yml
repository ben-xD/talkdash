default:
  app:
    name: Streetlights API Simplified
    version: 1.0.0

  broker:
    mqtt:
      url: mqtt://test.mosquitto.org
      topics: ["light/measured"]
      qos:
      protocol: mqtt
      retain:
      subscribe: true

development:

test:

staging:

production:
